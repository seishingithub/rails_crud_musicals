<p>
  <%= link_to 'Add Musical', new_musical_path %>
</p>

<div class="table">
  <table>
    <th>Name of Musical</th>
    <th>....</th>
    <th>Actor's Name</th>
    <% @musicals.each do |musical| %>
      <tr>
        <td><%= link_to "#{musical.name}", musical_path(musical) %></td>
        <td>....</td>
        <td><%= musical.actor %></td>
      </tr>
    <% end %>
  </table>
</div>